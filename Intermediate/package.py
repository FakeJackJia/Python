import package # if package is imported, a folder __pycache__ will be created
# if import package, will automatically run _init_.py file

print(type(package))

# Note: _init_.py is not used to define code, but used to import members of package
